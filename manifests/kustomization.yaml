apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  project: oauth-api

resources:
  - postgres-pv.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - app-deployment.yaml
  - app-service.yaml
  - app-ingress.yaml
  - network-policy.yaml
  - ../sealed-secrets/app-sealed-secret.yaml
  - ../sealed-secrets/postgres-sealed-secret.yaml
